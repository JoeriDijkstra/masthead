{
  "label": "Blog",
  "description": "Lists your published posts, with optional tag filtering. Set it as the homepage to make it your front page.",
  "metadata": [
    {
      "key": "layout",
      "label": "Page layout",
      "type": "select",
      "options": ["contained", "wide"],
      "default": "contained",
      "description": "Use 'wide' for a full-width post list; 'contained' centers a readable column."
    },
    {
      "key": "show_navigation",
      "label": "Show site navigation",
      "type": "boolean",
      "default": true,
      "description": "Untick to hide the navbar and footer on this page only."
    }
  ]
}
